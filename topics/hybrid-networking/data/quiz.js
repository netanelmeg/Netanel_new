const QUIZ_QUESTIONS = [
  {
    question: 'What does a VPN Gateway use to automatically learn on-prem routes — without you typing them manually?',
    options: ['BGP', 'NSG', 'Route Table', 'Azure Firewall'],
    correct: 0,
    highlightNodes: ['vpn-gw', 'bgp'],
    highlightArrows: ['vpngw-uses-bgp'],
    explanation: 'BGP (Border Gateway Protocol) lets the VPN Gateway and on-prem router exchange routes automatically. Without it, every new on-prem subnet means a manual update to the Local Network Gateway.',
  },
  {
    question: 'Which Azure resource represents the on-premises network from Azure\'s point of view?',
    options: ['VPN Gateway', 'Local Network Gateway', 'BGP', 'On-prem Router'],
    correct: 1,
    highlightNodes: ['lng', 'on-prem'],
    highlightArrows: ['lng-represents-onprem'],
    explanation: 'The Local Network Gateway is an Azure-side resource that *describes* on-prem: its public IP and address ranges (or BGP peer config). It doesn\'t run on-prem — it lives in Azure.',
  },
  {
    question: 'In a hub-and-spoke topology, how does traffic from Spoke-A reach Spoke-B?',
    options: ['Directly via VNet Peering', 'Through the Hub Firewall', 'Through the VPN Gateway', 'Via BGP routing'],
    correct: 1,
    highlightNodes: ['spoke-a', 'hub-vnet', 'firewall', 'spoke-b'],
    highlightArrows: ['spoke-a-peers', 'spoke-b-peers'],
    explanation: 'VNet peering is non-transitive — Spoke-A and Spoke-B can\'t talk directly even though both peer with the hub. The Route Table in each spoke forces inter-spoke traffic to the Hub Firewall, which then forwards it.',
  },
  {
    question: 'A VM in Spoke-A pings an on-prem server by IP — it works. Pinging by hostname fails. What\'s the most likely cause?',
    options: ['NSG blocking port 53', 'Route Table missing on-prem route', 'DNS conditional forwarder not configured', 'VPN tunnel is down'],
    correct: 2,
    highlightNodes: ['spoke-a', 'dns-zone', 'onprem-dns'],
    highlightArrows: ['spoke-a-dns-link', 'dns-forward'],
    explanation: '"Ping by IP works, name fails" is a DNS problem by definition. If the conditional forwarder pointing Azure DNS at the on-prem DNS server is missing, Azure has no idea how to resolve *.corp.local names.',
  },
  {
    question: 'The Private DNS Zone corp.local is a standalone Azure resource. How do VNets use it?',
    options: ['Each VNet gets its own copy', 'VNets link to the zone — one zone serves many VNets', 'The zone lives inside the Hub VNet', 'VNets auto-discover it via BGP'],
    correct: 1,
    highlightNodes: ['dns-zone', 'spoke-a', 'spoke-b', 'hub-vnet'],
    highlightArrows: ['spoke-a-dns-link', 'spoke-b-dns-link', 'hub-dns-link'],
    explanation: 'A Private DNS Zone is completely independent of any VNet. You link it to as many VNets as you want — they all share the same zone. That\'s the whole point: one zone for hub + all spokes.',
  },
  {
    question: 'In the route table entry "dest 10.2.0.0/16 → next hop: Hub FW", what device is the "Hub FW"?',
    options: ['VPN Gateway', 'BGP router', 'Azure Firewall', 'Local Network Gateway'],
    correct: 2,
    highlightNodes: ['firewall', 'spoke-a', 'spoke-b'],
    highlightArrows: [],
    explanation: 'Azure Firewall has a private IP (e.g. 10.0.1.4) that becomes the next-hop in spoke route tables. It\'s what makes the hub useful — without a device to forward inter-spoke traffic, non-transitive peering would block everything.',
  },
  {
    question: 'Which Azure resource does the VPN Gateway form a connection *with* to establish the Site-to-Site tunnel?',
    options: ['The on-prem router directly', 'The Local Network Gateway', 'The Hub VNet', 'The BGP peer on Azure side'],
    correct: 1,
    highlightNodes: ['vpn-gw', 'lng', 'onprem-router'],
    highlightArrows: ['tunnel'],
    explanation: 'In Azure, you create a *connection* between a VPN Gateway and a Local Network Gateway. The LNG tells the gateway where on-prem is (public IP + address space). The actual IPsec tunnel then forms to the on-prem router.',
  },
  {
    question: 'Why is putting workloads (VMs, app services) inside the Hub VNet a common mistake?',
    options: [
      'Hub VNets don\'t support subnets',
      'It\'s fine — it\'s just a naming convention',
      'It mixes infrastructure and workloads, making auditing and security harder',
      'Azure limits Hub VNets to 5 resources',
    ],
    correct: 2,
    highlightNodes: ['hub-vnet', 'firewall', 'vpn-gw'],
    highlightArrows: [],
    explanation: 'The hub is designed for *shared plumbing* only — firewall, VPN gateway, DNS. Mixing in workloads makes it harder to segment, audit, and control who can reach what. Workloads belong in spokes.',
  },
  {
    question: 'What happens if two spoke VNets have overlapping address spaces (e.g. both use 10.1.0.0/16)?',
    options: [
      'Azure automatically NATs traffic between them',
      'VNet Peering creation fails or traffic doesn\'t route correctly',
      'BGP resolves the conflict',
      'Only the newer spoke is affected',
    ],
    correct: 1,
    highlightNodes: ['spoke-a', 'spoke-b'],
    highlightArrows: [],
    explanation: 'Azure requires non-overlapping address spaces for peering. If they overlap, you either can\'t create the peering at all, or routing becomes ambiguous and traffic drops silently. Always plan IP ranges centrally before creating VNets.',
  },
  {
    question: 'Azure DNS needs to resolve fileserver01.corp.local for a VM in Spoke-A. It can\'t answer directly. What does it do?',
    options: [
      'Returns NXDOMAIN immediately',
      'Queries the public root DNS servers',
      'Forwards the query to the on-prem DNS server via the conditional forwarder',
      'Asks the VPN Gateway for the route',
    ],
    correct: 2,
    highlightNodes: ['dns-zone', 'onprem-dns', 'spoke-a'],
    highlightArrows: ['dns-forward', 'spoke-a-dns-link'],
    explanation: 'The Private DNS Zone is configured with a conditional forwarder: anything matching *.corp.local gets forwarded to the on-prem DNS server (over the VPN tunnel). The on-prem DNS then resolves it and returns the answer.',
  },
];
