const SCENARIOS = {
  'explore': {
    title: 'Explore mode',
    description: 'Tap any node on the map to see what it is, what it does, and common gotchas. Pick a scenario above to see traffic flow.',
    activeNodes: [],
    activeArrows: [],
  },
  'spoke-to-spoke': {
    tag: 'TRAFFIC FLOW',
    title: 'Spoke-A VM → Spoke-B VM',
    description: 'A VM in Spoke-A wants to reach a VM in Spoke-B at <code>10.2.5.10</code>. Even though both are peered to the hub, they can\'t talk directly. Watch what happens:',
    activeNodes: ['spoke-a', 'hub-vnet', 'firewall', 'spoke-b'],
    activeArrows: ['spoke-a-peers', 'spoke-b-peers'],
    steps: [
      'VM in Spoke-A initiates: "Send packet to <code>10.2.5.10</code>"',
      'Spoke-A\'s Route Table is consulted: dest <code>10.2.0.0/16</code> → next hop = Hub Firewall',
      'Packet leaves Spoke-A, enters the Hub via the peering link',
      'Azure Firewall inspects the packet against its rules. If allowed, forwards it',
      'Packet enters Spoke-B via the peering link, reaches the destination VM',
      'Return traffic follows the reverse path (Spoke-B → Firewall → Spoke-A)',
    ],
    gotcha: 'Without the route table entry pointing at the firewall, the packet would have no path to Spoke-B and silently drop. Peering alone is NOT enough.',
  },
  'spoke-to-onprem': {
    tag: 'TRAFFIC FLOW',
    title: 'Spoke-A VM → On-prem server (by IP)',
    description: 'A VM in Spoke-A wants to reach an on-prem server at <code>192.168.1.50</code>. Watch the path traverse Azure and cross the tunnel:',
    activeNodes: ['spoke-a', 'hub-vnet', 'firewall', 'vpn-gw', 'on-prem', 'onprem-router'],
    activeArrows: ['spoke-a-peers', 'tunnel'],
    steps: [
      'VM in Spoke-A initiates: "Send packet to <code>192.168.1.50</code>"',
      'Spoke-A\'s Route Table: dest <code>192.168.0.0/16</code> → next hop = Hub Firewall (learned via BGP)',
      'Packet enters Hub, Firewall inspects and allows it',
      'Packet routed to the VPN Gateway',
      'VPN Gateway encapsulates the packet into the IPsec tunnel',
      'Encrypted packet travels over the public internet to the on-prem router',
      'On-prem router decrypts, routes the packet to <code>192.168.1.50</code>',
    ],
    tip: 'Notice BGP\'s quiet role: the route on Spoke-A pointing to 192.168.0.0/16 was learned automatically. Without BGP, you\'d type it by hand.',
  },
  'dns-resolution': {
    tag: 'CONTROL PLANE',
    title: 'DNS: Azure VM resolves an on-prem name',
    description: 'An Azure VM tries to connect to <code>fileserver01.corp.local</code>. Before any data can flow, DNS has to resolve the name to an IP. Watch the query travel:',
    activeNodes: ['spoke-a', 'dns-zone', 'onprem-dns'],
    activeArrows: ['spoke-a-dns-link', 'dns-forward'],
    steps: [
      'VM in Spoke-A calls <code>fileserver01.corp.local</code>',
      'Query goes to Azure DNS (default DNS for the linked Private DNS Zone)',
      'Azure DNS doesn\'t own <code>corp.local</code> — but it has a conditional forwarder',
      'Query is forwarded to the on-prem DNS server (over the VPN tunnel)',
      'On-prem DNS resolves: <code>fileserver01.corp.local → 192.168.1.50</code>',
      'Answer travels back to the Azure VM',
      'NOW the VM can open a TCP connection to <code>192.168.1.50</code>',
    ],
    gotcha: 'If DNS forwarding isn\'t configured, you get the classic "ping by IP works but ping by name fails" symptom. It\'s ALWAYS DNS. 😅',
  },
  'internet-out': {
    tag: 'TRAFFIC FLOW',
    title: 'Spoke-A VM → Internet (forced tunneling)',
    description: 'A VM in Spoke-A wants to reach <code>api.weather.com</code>. In an enterprise hub-and-spoke, internet traffic doesn\'t go out directly — it\'s forced through the hub firewall first:',
    activeNodes: ['spoke-a', 'hub-vnet', 'firewall'],
    activeArrows: ['spoke-a-peers'],
    steps: [
      'VM in Spoke-A initiates: "Send packet to <code>api.weather.com</code>" (some public IP)',
      'Spoke-A\'s Route Table: dest <code>0.0.0.0/0</code> (everything else) → next hop = Hub Firewall',
      'Packet enters Hub, hits Azure Firewall',
      'Firewall inspects: is this destination/FQDN allowed?',
      'If allowed, firewall SNATs the packet (source becomes the firewall\'s public IP)',
      'Firewall sends the packet out to the internet',
      'Return traffic comes back to the firewall, gets routed back to the VM',
    ],
    tip: 'This pattern is called "forced tunneling." Every Azure VM\'s outbound internet traffic is inspected and logged by the firewall. Essential for compliance and security audits.',
  },
};
